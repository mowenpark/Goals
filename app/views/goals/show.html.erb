<%= @goal.title %>
<br>
<%= @goal.is_public ? "Public" : "Private" %>
<br>
<%= @goal.completed ? "Done" : "Working on it!" %>
<br>
<br>
<%= button_to "Edit", edit_goal_url(@goal), method: :get %>
<%= link_to 'Goals', user_url(@goal.user) %>
