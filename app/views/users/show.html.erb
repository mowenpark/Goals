<%= @user.username %> Goals
<br>
<% if current_user %>
  <% @user.goals.each do |goal| %>
    <%= link_to goal.title, goal_url(goal) %>
    <br>
  <% end %>
<% end %>
<%= button_to "New Goal", new_goal_url, method: :get %>
